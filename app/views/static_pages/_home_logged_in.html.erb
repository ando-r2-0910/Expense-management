<div class="profile_infomation">
  <div class="profile-header">
   
  </div>
  <div class="col-md-3 col-sm-12 profile-sidebar">
    <div class="profile-icon">
     
    </div>
    <div class="profile-name">
      <%= "#{current_user.name} さん" %>
    </div>
    <div class="profile-created_at">
      <%= "#{current_user.created_at.strftime('%Y年%m月%d')}日に登録" %>
    </div>
    <%= render("static_pages/book_record_form") %>
  </div>
  <div class="col-md-9 col-sm-12 profile-information">
    <%= render("shared/carousel", user: current_user) %>
  <div>
</div>